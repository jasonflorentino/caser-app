import { Title } from '@mantine/core';

import "./Header.scss";

const Header = () => {
  return (
    <header className="Header">
      <Title order={1}>Caser 💼</Title>
    </header>
  )
}

export default Header;